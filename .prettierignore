node_modules/
.next/
dist/
coverage/
.expo/
pnpm-lock.yaml

# Vendored and generated output. Prettier must not own these.
apps/mobile/ios/Pods/
apps/mobile/expo-env.d.ts

# Generated by `pnpm openapi:generate`, and byte-compared by openapi.test.ts
# against `JSON.stringify(doc, null, 2)`. Prettier collapses short arrays that
# JSON.stringify expands, so formatting this file makes the drift gate
# unsatisfiable — `pnpm test` and `pnpm format:check` cannot both pass. The
# generator owns the bytes; prettier stays out.
services/api/openapi.generated.json
